# Base URL of the deployed backend API. Set this in the Cloudflare Pages project
# (Settings → Environment variables) — it is baked in at BUILD time, so changing it
# requires a rebuild. Leave it UNSET for local dev (Vite proxies to localhost:7700).
VITE_API_URL=https://summit-email-backend.onrender.com
